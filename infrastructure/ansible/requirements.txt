# Install into the ansible-core pipx environment with:
# pipx runpip ansible-core install -r infrastructure/ansible/requirements.txt
google-auth>=2.0.0
requests>=2.25.0
boto3>=1.34.0
botocore[crt]>=1.34.0
