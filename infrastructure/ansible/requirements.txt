# Controller-side Python requirements for the OilScope Ansible tooling.
#
# The GCP and AWS inventory plugins use the libraries declared below. Azure's
# SDK dependencies are maintained by azure.azcollection itself and must be
# installed from that collection's requirements.txt after ansible-galaxy:
#
#     pip install -r infrastructure/ansible/requirements.txt
#     ansible-galaxy collection install -r infrastructure/ansible/requirements.yml
#     pip install -r ~/.ansible/collections/ansible_collections/azure/azcollection/requirements.txt
#     gcloud auth application-default login
#
ansible-core==2.21.4
ansible-lint==26.8.0
yamllint>=1.35,<2

boto3>=1.34,<2
google-auth>=2,<3
requests>=2.25,<3
