# Controller-side Python requirements for the OilScope Ansible tooling.
#
# The multi-cloud inventory wrapper delegates to the GCP and AWS inventory
# plugins. These are their controller-side Python dependencies:
#
#     pip install -r infrastructure/ansible/requirements.txt
#     gcloud auth application-default login
#
google-auth>=2.0.0
requests>=2.25.0
boto3>=1.34.0
botocore>=1.34.0
