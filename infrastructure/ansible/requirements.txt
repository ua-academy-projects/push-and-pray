# Controller-side Python requirements for the OilScope Ansible tooling.
#
# The google.cloud.gcp_compute inventory plugin used by inventory/oilscope.yml
# talks to the Compute Engine API through these libraries, authenticating
# with Application Default Credentials:
#
#     pip install -r infrastructure/ansible/requirements.txt
#     gcloud auth application-default login
#
# The amazon.aws.aws_ec2 inventory plugin, also used by inventory/oilscope.yml,
# talks to the EC2 API through boto3/botocore, authenticating with the
# standard AWS credential chain (environment variables, a named profile, or
# an attached role):
#
#     aws configure
#
google-auth>=2.0.0
requests>=2.25.0
boto3>=1.34.0
botocore>=1.34.0
