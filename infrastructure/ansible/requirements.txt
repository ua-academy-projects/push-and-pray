# Controller-side Python requirements for the OilScope Ansible tooling.
#
# The multi-cloud inventory wrapper delegates discovery to gcp_compute and
# aws_ec2. These are the controller-side Python libraries they import:
#
#     pip install -r infrastructure/ansible/requirements.txt
#     gcloud auth application-default login
#
google-auth>=2.0.0
requests>=2.25.0
boto3>=1.34.0
botocore>=1.34.0
