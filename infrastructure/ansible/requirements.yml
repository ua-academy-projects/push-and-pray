# SPDX-License-Identifier: GPL-2.0-or-later
---
# Provider collections used by the OilScope cloud inventory plugin.
#
#   ansible-galaxy collection install -r infrastructure/ansible/requirements.yml
collections:
  - name: google.cloud
    version: ">=1.14.0"
  - name: amazon.aws
    version: ">=10.0.0"
