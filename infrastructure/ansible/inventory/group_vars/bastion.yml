# SPDX-License-Identifier: GPL-2.0-or-later
---
bastion_ssh_port: "{{ oilscope_bastion_ssh_port | int }}"

ansible_port: >-
  {{ lookup('ansible.builtin.env', 'OILSCOPE_BASTION_CONNECT_PORT')
     | default(bastion_ssh_port, true) | int }}
