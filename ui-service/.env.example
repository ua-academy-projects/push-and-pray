# UI Service configuration.
# Copy to .env and adjust if the Backend runs on a different host/port.
# Never commit a real .env file.

# The only backend the UI is allowed to know about.
VITE_BACKEND_BASE_URL=http://localhost:8000
