.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.DS_Store
htmlcov/
.coverage
node_modules/
infrastructure/docker/deployment

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
infrastructure/terraform/terraform.tfvars
infrastructure/terraform/legacy
infrastructure/legacy_terraform
*.tfvars
*.tfvars.json
*.tfplan
**/tfplan
crash.log
crash.*.log
project-config.json
project-config.*.json
!infrastructure/terraform/project-config.schema.json
# Credentials and private keys
*-key.json
*.pem
*.key
!project-config.example.json

# Built Ansible collection artifacts
infrastructure/ansible/**/*.tar.gz
infrastructure/ansible/inventory/local.oilscope.yml

# Archived Vagrant snapshot runtime files (the legacy implementation is not active).
**/.vagrant/
infrastructure/legacy/vagrant/config/vagrant.env

# Legacy provider-specific local inventory name retained to prevent accidental
# commits from pre-multi-cloud worktrees.
*.local.gcp.yml
