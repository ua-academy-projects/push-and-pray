# Root-level overview only. Each service owns its real configuration —
# see ui-service/.env.example, backend-service/.env.example, history-service/.env.example.
#
# Default local ports used throughout the docs:
#   ui-service       -> 5173 (Vite dev server)
#   backend-service  -> 8000
#   history-service  -> 8001
#   PostgreSQL       -> 5432
